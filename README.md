# uloader

uloader is a small bootloader for [OpenComputers](https://ocdoc.cil.li/) computers.

## Installation

To install uloader, put the uloader folder on the root of the boot drive and flash init.lua to the eeprom. The eeprom's data should contain the address of the drive with the uloader folder on it.
